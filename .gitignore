# Binaries
/runops-gateway
/runops

# Go build cache
*.test
*.out

# Environment
.env
.env.*

# OS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# OpenTofu / Terraform
tofu/.terraform/
tofu/.terraform.lock.hcl
tofu/*.tfvars
tofu/terraform.tfstate
tofu/terraform.tfstate.backup
tofu/override.tf

# Expedition state files (paintress)
journal.tsv
gradient.json
lumina.md
gommage.md
continent-config.yaml

# Local-only working docs — handover / intent are kept in the working tree
# but not committed (per 2026-05-31 decision). Edit them locally; they no
# longer travel with the repo.
docs/handover.md
docs/intent.md
